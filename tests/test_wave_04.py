# There are no tests for wave 4.

# What is the responsibility of this endpoint? chat.postMessage
# What is the URL and HTTP method for this endpoint? POST
# What are the two required arguments for this endpoint? token, channel
# How does this endpoint relate to the Slackbot API key (token) we just created?
# Authentication token bearing required scopes. Tokens should be passed as an HTTP Authorization header or alternatively, as a POST parameter.

